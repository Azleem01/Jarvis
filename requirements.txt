google-genai>=1.0.0
faster-whisper>=1.0.0
sounddevice>=0.4.6
numpy>=1.24
pynput>=1.7.6
pyautogui>=0.9.54
# Pulled in by pyautogui, but imported directly (waiting for a window to focus),
# so it is pinned here rather than relied on transitively.
pygetwindow>=0.0.9
mss>=9.0.1
Pillow>=10.0.0
python-dotenv>=1.0.0
httpx>=0.24.0
pycaw>=20240210
